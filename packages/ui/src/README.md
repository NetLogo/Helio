# NetLogo UI Library

## Installation
1. Add `@repo/tailwind-config` and `@repo/ui` to your app
```bash
yarn workspace <my-project> add @repo/tailwind-config @repo/ui
```
2. Add `@import "@repo/tailwind-config";` to your root SASS file.