{
  "name": "@repo/emails",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build":  "yarn run emit-types -p tsconfig.build.json && npx esbuild src/index.tsx --bundle --minify --sourcemap --target=esnext --format=esm --outfile=dist/index.js",
    "emit-types": "tsc --emitDeclarationOnly --outDir dist",
    "dev": "email dev --dir src/templates",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@react-email/components": "^0.0.32",
    "@react-email/render": "^1.0.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "react-email": "^3.0.4",
    "typescript": "^5.6.3"
  }
}
