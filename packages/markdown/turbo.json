{
  "$schema": "https://v2-8-20.turborepo.dev/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "dependsOn": [
        "build:styles",
        "build:components",
        "@repo/utils#build",
        "^build"
      ]
    },
    "build:styles": {
      "outputs": [
        "dist/**",
        "^build"
      ]
    },
    "build:components": {
      "outputs": [
        "dist/**",
        "^build"
      ]
    }
  }
}